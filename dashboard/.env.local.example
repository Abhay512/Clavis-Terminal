# Copy to .env.local

# true  -> render generated fixture data, no backend required
# false -> connect to the live screener
NEXT_PUBLIC_DEMO=false

# WebSocket endpoint exposed by the Python backend
NEXT_PUBLIC_API_WS=ws://localhost:8000/ws/live
